import React from "react";

const ButtonMain = ({ tag }) => {
  return (
    <button className="button_1_button">
      <span class="button_span">{tag}</span>
    </button>
  );
};

export default ButtonMain;
